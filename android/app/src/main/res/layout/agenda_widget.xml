<?xml version="1.0" encoding="utf-8"?>
<!-- El papel (sombra, franja, esquina doblada) es un Bitmap que dibuja AgendaWidgetProvider en widget_paper;
     el padding deja el texto dentro del papel, que a su vez deja margen a la sombra -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/widget_paper"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY"
        android:importantForAccessibility="no" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingLeft="20dp"
        android:paddingTop="22dp"
        android:paddingRight="20dp"
        android:paddingBottom="28dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/widget_prev"
                android:layout_width="28dp"
                android:layout_height="32dp"
                android:gravity="center"
                android:includeFontPadding="false"
                android:text="‹"
                android:textSize="28sp"
                android:textStyle="bold"
                android:textColor="@color/widget_text"
                android:contentDescription="@string/widget_prev_day" />

            <TextView
                android:id="@+id/widget_date"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:gravity="center"
                android:paddingLeft="2dp"
                android:paddingRight="2dp"
                android:text="Lunes, 21 sep"
                android:textSize="17sp"
                android:textStyle="bold"
                android:textColor="@color/widget_text"
                android:maxLines="1"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/widget_next"
                android:layout_width="28dp"
                android:layout_height="32dp"
                android:gravity="center"
                android:includeFontPadding="false"
                android:text="›"
                android:textSize="28sp"
                android:textStyle="bold"
                android:textColor="@color/widget_text"
                android:contentDescription="@string/widget_next_day" />

            <TextView
                android:id="@+id/widget_add"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:layout_marginLeft="6dp"
                android:gravity="center"
                android:includeFontPadding="false"
                android:background="@drawable/widget_add_button"
                android:text="+"
                android:textSize="22sp"
                android:textStyle="bold"
                android:textColor="@color/widget_accent"
                android:contentDescription="@string/widget_add_task" />
        </LinearLayout>

        <TextView
            android:id="@+id/widget_progress"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="6dp"
            android:gravity="center"
            android:text="Hoy · 1 de 4 completadas"
            android:textSize="12sp"
            android:textColor="@color/widget_muted"
            android:maxLines="1"
            android:ellipsize="end" />

        <TextView android:id="@+id/widget_task1" style="@style/WidgetTaskRow" />
        <TextView android:id="@+id/widget_task2" style="@style/WidgetTaskRow" />
        <TextView android:id="@+id/widget_task3" style="@style/WidgetTaskRow" />
        <TextView android:id="@+id/widget_task4" style="@style/WidgetTaskRow" />
        <TextView android:id="@+id/widget_task5" style="@style/WidgetTaskRow" />
        <TextView android:id="@+id/widget_task6" style="@style/WidgetTaskRow" />
        <TextView android:id="@+id/widget_task7" style="@style/WidgetTaskRow" />
        <TextView android:id="@+id/widget_task8" style="@style/WidgetTaskRow" />

    </LinearLayout>

</FrameLayout>
